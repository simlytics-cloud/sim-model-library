openapi: 3.1.0

info:
  title: Remote Model Library API
  version: 1.0.0

paths:

  /v1/models:
    get:
      summary: List available models
      operationId: listModels
      responses:
        '200':
          description: Available models
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ModelListResponse'

  /v1/models/{modelId}:
    get:
      summary: Get model definition
      operationId: getModel
      parameters:
        - $ref: '#/components/parameters/ModelId'
      responses:
        '200':
          description: Model definition
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Model'
        '404':
          $ref: '#/components/responses/NotFound'

  /v1/models/{modelId}/run:
    put:
      summary: Start a remote model runner
      operationId: startModelRun
      parameters:
        - $ref: '#/components/parameters/ModelId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/StartModelRunRequest'
      responses:
        '202':
          description: Remote model-run request accepted
          headers:
            Location:
              description: URL for polling run status
              schema:
                type: string
                example: /v1/runs/run-001
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StartModelRunResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '409':
          description: The model library has already seen this runId. Retries are rejected.
        '404':
          $ref: '#/components/responses/NotFound'

  /v1/runs:
    get:
      summary: List all runs
      operationId: listRuns
      responses:
        '200':
          description: List of run statuses
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/RunStatusResponse'

  /v1/runs/{runId}:
    get:
      summary: Get run status
      operationId: getRunStatus
      parameters:
        - $ref: '#/components/parameters/RunId'
      responses:
        '200':
          description: Run status
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RunStatusResponse'
        '404':
          $ref: '#/components/responses/NotFound'
    delete:
      summary: Cancel run
      operationId: deleteRun
      parameters:
        - $ref: '#/components/parameters/RunId'
      responses:
        '200':
          description: Run status after cancel request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RunStatusResponse'
        '404':
          $ref: '#/components/responses/NotFound'

  /v1/run-config/defaults:
    get:
      summary: Get default run configuration values
      operationId: getRunConfigDefaults
      responses:
        '200':
          description: Default Kafka values for run configuration
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/KafkaDefaultsResponse'

components:

  parameters:
    ModelId:
      name: modelId
      in: path
      required: true
      schema:
        type: string
    RunId:
      name: runId
      in: path
      required: true
      schema:
        type: string

  schemas:

    ModelListResponse:
      type: object
      required:
        - models
      properties:
        models:
          type: array
          items:
            $ref: '#/components/schemas/ModelSummary'

    ModelSummary:
      type: object
      required:
        - modelId
        - name
        - description
        - implementationLanguage
      properties:
        modelId:
          type: string
          example: battery-model-v1
        name:
          type: string
          example: Battery Energy Model
        description:
          type: string
        timeMode:
          $ref: '#/components/schemas/TimeMode'
        implementationLanguage:
          type: string
          example: Java

    Model:
      type: object
      required:
        - modelId
        - name
        - description
        - inputPorts
        - outputPorts
        - parameters
        - implementationLanguage
        - messageSchemas
        - initializationSchema
      properties:
        modelId:
          type: string
        name:
          type: string
        description:
          type: string
        implementationLanguage:
          type: string
          example: Java

        inputPorts:
          type: array
          items:
            $ref: '#/components/schemas/PortDefinition'

        outputPorts:
          type: array
          items:
            $ref: '#/components/schemas/PortDefinition'

        parameters:
          type: array
          description: High-level list of model initialization parameters.
          items:
            $ref: '#/components/schemas/ParameterDefinition'

        messageSchemas:
          type: object
          description: JSON Schemas for port message payloads.
          additionalProperties:
            $ref: '#/components/schemas/JsonSchema'

        initializationSchema:
          $ref: '#/components/schemas/JsonSchema'

        defaultParameterSet:
          $ref: '#/components/schemas/ModelDefaultParameterSet'

        behaviorDescription:
          type: string
          description: Optional code, pseudo-code, or behavioral explanation.

        metadata:
          type: object
          additionalProperties: true

    PortDefinition:
      type: object
      required:
        - name
        - direction
        - messageType
      properties:
        name:
          type: string
          example: powerDemand
        direction:
          type: string
          enum: [input, output]
        messageType:
          type: string
          example: BatteryPowerRequest
        description:
          type: string

    ParameterDefinition:
      type: object
      required:
        - name
        - type
        - required
      properties:
        name:
          type: string
          example: initialStateOfCharge
        type:
          type: string
          example: number
        required:
          type: boolean
        defaultValue: {}
        description:
          type: string

    StartModelRunRequest:
      type: object
      required:
        - runId
        - initializationParameters
        - kafka
        - simulation
      properties:
        runId:
          type: string
          description: Immutable identifier created and supplied by the coordinator or its orchestrator. The model library never generates it and rejects every duplicate.
          example: run-001

        initializationParameters:
          type: object
          description: |
            Model-specific initialization values. This object must validate
            against the model's initializationSchema.
          additionalProperties: true

        kafka:
          $ref: '#/components/schemas/KafkaConfiguration'

        simulation:
          $ref: '#/components/schemas/SimulationContext'

        coordinatorHelper:
          $ref: '#/components/schemas/CoordinatorHelperCallback'

    KafkaConfiguration:
      type: object
      description: |
        Caller-supplied Kafka transport settings for the remote model runner.
        The consumer group ID is not caller-configurable; at runtime it is always
        derived as `runId:receiverId`, giving each receiver stable run-scoped offset
        tracking. A supplied `group.id` is ignored in favor of that derived value.
      required:
        - topic
        - properties
      properties:
        topic:
          type: string
          description: |
            Shared Kafka topic reused across runs. Per-run isolation is achieved via
            runId-based record keying and `X-Run-Id` header filtering, not by creating
            dedicated per-run topics.
          example: simulation.run.001
        properties:
          type: object
          description: Native Kafka client properties. `bootstrap.servers` is required.
          additionalProperties:
            type: string

    SimulationContext:
      type: object
      required: [simulationId, modelInstanceId, coordinatorId, timeMode]
      properties:
        simulationId:
          type: string
          example: sim-001
        modelInstanceId:
          type: string
          example: instance-001
          pattern: '^[A-Za-z0-9][A-Za-z0-9._-]{0,127}$'
        coordinatorId:
          type: string
          example: mission-coordinator-alpha
          pattern: '^[A-Za-z0-9][A-Za-z0-9._-]{0,127}$'
        timeMode:
          $ref: '#/components/schemas/TimeMode'

    CoordinatorHelperCallback:
      type: object
      description: Optional HTTP callback to a separately deployed coordinator helper. It is not part of the Kafka simulation transport.
      required: [endpoint, token]
      properties:
        endpoint:
          type: string
          format: uri
        token:
          type: string
          writeOnly: true

    TimeModeType:
      type: string
      description: Runtime pacing strategy independent of common logical time.
      enum: [real-time, scaled-real-time, virtual-time]

    TimeMode:
      type: object
      description: Required runtime pacing configuration. Logical time uses the common unit-one JSON-number representation.
      required:
        - mode
      properties:
        mode:
          $ref: '#/components/schemas/TimeModeType'
          description: Runtime pacing mode independent from logical time representation.
        realTimeFactor:
          type: [number, 'null']
          format: double
          description: Required for `scaled-real-time`; otherwise null or omitted. Scaling factor for `scaled-real-time` mode (for example, `2.0` runs at 2x wall-clock speed).
          example: 2.0
      allOf:
        - if:
            properties:
              mode:
                const: scaled-real-time
          then:
            required: [realTimeFactor]

    StartModelRunResponse:
      type: object
      required:
        - runId
        - modelId
        - status
        - statusUrl
      properties:
        runId:
          type: string
          description: Coordinator-supplied run identifier for remote-run status polling.
          example: run-7f83a9c2
        modelId:
          type: string
          example: irpsystem.irpmodel.Vehicle
        status:
          $ref: '#/components/schemas/RunStatus'
        statusUrl:
          type: string
          description: Relative URL to retrieve current run status.
          example: /v1/runs/run-7f83a9c2
        acceptedAt:
          type: string
          format: date-time
          description: Timestamp when the model library accepted the remote model-run request.
          example: '2026-06-14T12:00:00Z'
        message:
          type: string

    RunStatusResponse:
      type: object
      required:
        - runId
        - modelId
        - status
      properties:
        runId:
          type: string
          example: run-7f83a9c2
        modelId:
          type: string
          example: irpsystem.irpmodel.Vehicle
        simulationId:
          type: string
        modelInstanceId:
          type: string
        coordinatorId:
          type: string
        status:
          $ref: '#/components/schemas/RunStatus'
        acceptedAt:
          type: string
          format: date-time
          example: '2026-06-14T12:00:00Z'
        readyAt:
          type: string
          format: date-time
          description: Set when the remote model runner signals local readiness. Coordinated readiness belongs to an optional coordinator helper.
          example: '2026-06-14T12:00:05Z'
        startedAt:
          type: string
          format: date-time
          example: '2026-06-14T12:00:06Z'
        completedAt:
          type: string
          format: date-time
          example: '2026-06-14T12:03:20Z'
        message:
          type: string
        currentSimulationTime:
          $ref: '#/components/schemas/CurrentSimulationTime'

    KafkaDefaultsResponse:
      type: object
      description: |
        Default Kafka connection values for run startup.
        Consumer group ID is not included because it is derived at runtime as
        `runId:receiverId` and is not caller-configurable.
      required:
        - topic
        - properties
      properties:
        topic:
          type: string
          example: devs-sim
        properties:
          type: object
          description: Native Kafka client properties, including `bootstrap.servers`.
          additionalProperties:
            type: string

    CurrentSimulationTime:
      type: object
      required:
        - value
        - sourceMessageType
        - updatedAt
      properties:
        value:
          type: number
          description: Common logical time as a JSON number. Participants agree on Integer or Real representation and a unit length of one.
          example: 125.0
        sourceMessageType:
          type: string
          example: NextInternalTimeReport
        sourceMessageId:
          type: string
          example: message-123
        updatedAt:
          type: string
          format: date-time
          example: '2026-07-01T12:34:56Z'

    RunStatus:
      type: string
      description: Remote model-runner lifecycle state. Coordinated lifecycle is available only from an optional coordinator helper.
      enum: [accepted, starting, locally-ready, locally-stopped, locally-failed]

    JsonSchema:
      type: object
      description: Embedded JSON Schema 2020-12 document.
      additionalProperties: true

    ModelDefaultParameterSet:
      type: object
      description: |
        Model-specific initialization default values.
        This object must validate against the model's initializationSchema.
      additionalProperties: true

    ErrorResponse:
      type: object
      required: [code, message]
      properties:
        code:
          type: string
        message:
          type: string

  responses:
    BadRequest:
      description: Invalid request
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'

    NotFound:
      description: Resource not found
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'

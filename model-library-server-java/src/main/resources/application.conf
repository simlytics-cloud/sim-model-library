model.library {
  server.port = 8090
  runtime.executor = "stub"
  run-config.kafka-defaults {
    bootstrap-servers = "localhost:9092"
    topic = "devs-sim"
    security-protocol = "PLAINTEXT"
    sasl-mechanism = ""
  }
}